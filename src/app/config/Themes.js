import colors from './colors'

export const darkTheme = {
    body: colors.primary.dark,
    text: colors.primary.light
}

export const lightTheme = {
    body: colors.primary.light,
    text: colors.primary.dark
}